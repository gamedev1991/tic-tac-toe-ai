# Server Configuration
PORT=3001
NODE_ENV=development

# Optional: CORS Configuration
# CORS_ORIGIN=http://localhost:3000

# Note: In production (Railway), PORT will be automatically set
# and NODE_ENV should be set to 'production' 